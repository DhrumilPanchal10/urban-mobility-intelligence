urban_mobility:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: airflow
      pass: airflow
      port: 5432
      dbname: airflow
      schema: analytics
      threads: 4
